gem 'rspec', :require => false     # RubyMine detects when RSpec is added at the top level of the Gemfile

group :development, :test do
  gem 'rspec', :require => false    # RubyMine does not detect when RSpec is added under a group within the Gemfile
end